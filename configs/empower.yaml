default_mode: "refined"
frame_id: "gemini336_color_optical_frame"

visualization:
  voxel_size: 0.01
  marker_radius: 0.03
  show_window: true

mode:
  original:
    relation_offset_m: 0.15
    detector_backend: "sam3"
    false_detection_fallback: false
  refined:
    relation_offset_m: 0.15
    detector_backend: "sam3"
    false_detection_fallback: true
